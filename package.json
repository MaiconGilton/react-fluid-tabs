{
  "name": "react-swipeable-tabs-monorepo",
  "private": true,
  "workspaces": [
    "react-swipeable-tabs",
    "docs"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=docs",
    "dev:package": "npm run dev --workspace=react-swipeable-tabs",
    "build": "npm run build --workspaces",
    "lint": "npx @biomejs/biome lint .",
    "format": "npx @biomejs/biome format --write .",
    "check": "npx @biomejs/biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8"
  }
}